# 🚀 Руководство по развертыванию

## Автоматическая инициализация БД

Приложение автоматически инициализирует базу данных при старте сервера через server plugin `server/plugins/init-database.ts`.

### Что происходит при старте:

1. **Проверка директории БД** - создается директория для файла БД, если её нет
2. **Выполнение миграций** - автоматически применяются миграции Prisma
3. **Создание БД** - если БД не существует, она создается через `prisma db push`
4. **Проверка подключения** - тестируется подключение к БД

## Настройка для Render.com

### 1. Переменные окружения

В настройках сервиса на Render добавьте:

```env
DATABASE_URL="file:./prisma/dev.db"
NODE_ENV=production
```

**Важно:** На Render файловая система может быть временной. Рекомендуется использовать PostgreSQL вместо SQLite для production.

### 2. Build Command

```bash
npm install && npm run build
```

### 3. Start Command

```bash
npm start
```

### 4. Postinstall Script

В `package.json` уже настроен `postinstall`, который:
- Генерирует Prisma Client (`prisma generate`)
- Подготавливает Nuxt (`nuxt prepare`)

## Настройка для других платформ

### Heroku

```env
DATABASE_URL="file:./prisma/dev.db"
NODE_ENV=production
```

**Важно:** Heroku использует ephemeral filesystem. SQLite не подходит для production на Heroku. Используйте PostgreSQL.

### Vercel

Vercel не поддерживает SQLite из-за read-only filesystem. Используйте внешнюю БД (PostgreSQL, MySQL).

### Railway

```env
DATABASE_URL="file:./prisma/dev.db"
NODE_ENV=production
```

Railway поддерживает постоянное хранилище, SQLite может работать.

## Рекомендации для Production

### ⚠️ SQLite в Production

SQLite подходит для:
- ✅ Небольших проектов
- ✅ Single-instance приложений
- ✅ Платформ с постоянным хранилищем (Railway, DigitalOcean)

SQLite НЕ подходит для:
- ❌ Heroku (ephemeral filesystem)
- ❌ Vercel (read-only filesystem)
- ❌ Многопоточных приложений с высокой нагрузкой

### ✅ Рекомендуемое решение: PostgreSQL

Для production рекомендуется использовать PostgreSQL:

1. **Обновите `prisma/schema.prisma`:**
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

2. **Установите переменную окружения:**
```env
DATABASE_URL="postgresql://user:password@host:5432/dbname"
```

3. **Миграции выполнятся автоматически** при старте через server plugin.

## Логи и отладка

При старте сервера вы увидите логи:

```
🚀 === Инициализация базы данных ===
📂 Путь к БД: /path/to/prisma/dev.db
📁 Создаю директорию для БД: /path/to/prisma
🔄 Выполняю миграции Prisma...
✅ Миграции выполнены успешно
✅ База данных инициализирована успешно
✅ Подключение к БД успешно
```

Если есть ошибки, они будут логироваться в консоль.

## Ручная инициализация

Если автоматическая инициализация не сработала, можно выполнить вручную:

```bash
# Создать директорию
mkdir -p prisma

# Применить миграции
npx prisma migrate deploy

# Или создать БД через db push
npx prisma db push

# Сгенерировать Prisma Client
npx prisma generate
```

## Проверка работоспособности

После развертывания проверьте:

1. **Логи сервера** - должны быть сообщения об успешной инициализации БД
2. **API эндпоинты** - `/api/events` должен возвращать данные
3. **Мониторинг** - `/api/monitoring/:eventId` должен работать

Если есть ошибки "Unable to open the database file":
- Проверьте права на запись в директорию БД
- Убедитесь, что `DATABASE_URL` указан правильно
- Проверьте, что директория существует и доступна для записи

